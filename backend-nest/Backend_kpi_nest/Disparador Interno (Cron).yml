info:
  name: Disparador Interno (Cron)
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/internal/cerrar-vencidos
  headers:
    - name: x-cron-secret
      value: mi_secreto_super_seguro_para_hostinger
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
